import 'package:flutter/material.dart';

class AppScaffoldViewStyle {
  static const BorderRadiusGeometry borderRadiusBody =
      BorderRadius.all(Radius.circular(16.0));

  static const double appBarSize = 80;
}
