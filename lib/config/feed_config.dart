class FeedConfig {
  static const String roomType = 'app.twake.chat.feed';

  /// As of now, creating a feed requires a homeserver that supports this preset.
  static const String preset = 'public_channel';
}
